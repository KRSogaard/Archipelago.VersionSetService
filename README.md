Version Set Service Repo

Run
```
gradlew VersionSetService:bootRun
```

publish by running
```
gradlew VersionSetService.Client:publish
```

Remember to set the `gradle.properties` file in `~/.gradle/` or `C:\Users\<USER>\.gradle`
```
weweloRepoUser=xxx
weweloRepoPassword=xxx
```
